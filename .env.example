# Copy this file to .env in the project root before running `docker compose up`.
# docker-compose.yml reads this to set the MySQL root password consistently
# across the db and backend services.
MYSQL_ROOT_PASSWORD=change-me-in-production
